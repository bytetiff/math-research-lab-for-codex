# Formalization Protocol

Extract objects, domains, codomains, dimensions, assumptions, dependencies, inputs, outputs, and claim types before judging. Preserve ambiguity as an explicit unresolved item.
