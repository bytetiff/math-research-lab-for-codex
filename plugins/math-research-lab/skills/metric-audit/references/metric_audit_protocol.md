# Metric Audit Protocol

Use the user-provided definition. Check schema, indexing, exclusions, aggregation, duplicates, missing cells, baselines, final indices, and decomposition validity. A metric result is not a mechanism.
